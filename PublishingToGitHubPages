Publishing the website

Make changes to files

In console, render with
quarto::quarto_render()

In terminal (git bash) do the following:

git add docs/
git add .
git commit -m "NOTE"
git push -u origin main